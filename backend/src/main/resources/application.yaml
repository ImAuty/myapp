spring:
  application:
    name: myapp
  datasource:
    url: jdbc:postgresql://localhost:5432/myapp_db
    username: auty
    password: auty
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
